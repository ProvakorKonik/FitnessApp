<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stayfit.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StayFit">
        <activity android:name=".View.Profile"></activity>
        <activity android:name=".View.PerformDetails" />
        <activity android:name=".View.PerformAdd" />
        <activity android:name=".View.DayOne" />
        <activity android:name=".View.DayAdd" />
        <activity android:name=".LoginRegistration" />
        <activity android:name=".LoginCheck" />
        <activity android:name=".View.DayMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>